export class TodoVo { /** named export , : 다음 자료형이면 typescript, 클래스 다음에 오면 class 속성이다. */
  todo_id: number;
  isFinished: boolean;
  todo: string;
  created: string;
  updated: string;
  isEdited: boolean;
}

